# Junglee

## Build Setup

``` bash
# install dependencies
npm install

# build for production with minification
npm run build

# To run in localhost (http://localhost:8000/dist/)
npm run start
    > "Simple python server is used, so make sure you have python installed in your system, if not use any server and host dist directory"